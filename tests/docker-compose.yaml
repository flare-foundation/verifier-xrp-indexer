version: '3'

services:
  postgresdb:
    container_name: xrp_verifier_db
    image: postgres
    environment:
      POSTGRES_DB: "flare_xrp_indexer"
      POSTGRES_USER: "username"
      POSTGRES_PASSWORD: "password"
    ports:
      - "127.0.0.1:5432:5432"

